name: "PR Check"

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/**"
      - "src-tauri/src/**"
      - "src-tauri/Cargo.toml"
      - "src-tauri/Cargo.lock"
      - "src-tauri/build.rs"
      - "src-tauri/tauri.conf.json"
      - "src-tauri/tauri.*.conf.json"
      - "src/**"
      - "package.json"
      - "bun.lockb"
      - "bunfig.toml"
      - "tsconfig.json"
      - "vite.config.ts"
      - "biome.json"

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  # 检测文件变化类型
  detect-changes:
    name: "Detect Changes"
    runs-on: ubuntu-22.04
    outputs:
      frontend: ${{ steps.changes.outputs.frontend }}
      backend: ${{ steps.changes.outputs.backend }}
      workflow: ${{ steps.changes.outputs.workflow }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: changes
        with:
          filters: |
            frontend:
              - 'src/**'
              - 'package.json'
              - 'bun.lockb'
              - 'bunfig.toml'
              - 'tsconfig.json'
              - 'vite.config.ts'
              - 'biome.json'
            backend:
              - 'src-tauri/src/**'
              - 'src-tauri/Cargo.toml'
              - 'src-tauri/Cargo.lock'
              - 'src-tauri/tauri.conf.json'
              - 'src-tauri/tauri.*.conf.json'
              - 'src-tauri/build.rs'
            workflow:
              - '.github/workflows/**'

  frontend-check:
    name: "Frontend Code Check"
    runs-on: ubuntu-22.04
    needs: detect-changes
    if: needs.detect-changes.outputs.frontend == 'true' || needs.detect-changes.outputs.workflow == 'true'
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Cache frontend dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.bun
            node_modules/
          key: ${{ runner.os }}-frontend-${{ hashFiles('**/bun.lockb', '**/package.json') }}
          restore-keys: |
            ${{ runner.os }}-frontend-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run Biome check
        run: bun run check

      - name: TypeScript check
        run: bunx tsc --noEmit

      - name: Build frontend
        run: bun run build:vite

  rust-check:
    name: "Rust Code Check"
    needs: detect-changes
    if: needs.detect-changes.outputs.backend == 'true' || needs.detect-changes.outputs.workflow == 'true'
    strategy:
      fail-fast: false
      matrix:
        platform:
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
          - os: windows-latest
            target: x86_64-pc-windows-msvc
          - os: macos-latest
            target: x86_64-apple-darwin
          - os: macos-latest
            target: aarch64-apple-darwin

    runs-on: ${{ matrix.platform.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform.target }}

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            src-tauri/target/release/deps
            src-tauri/target/release/build
            src-tauri/target/${{ matrix.platform.target }}/release/deps
            src-tauri/target/${{ matrix.platform.target }}/release/build
          key: ${{ runner.os }}-${{ matrix.platform.target }}-rust-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.platform.target }}-rust-

      - name: Cache system dependencies
        if: matrix.platform.os == 'ubuntu-22.04'
        uses: actions/cache@v4
        with:
          path: /var/cache/apt
          key: ${{ runner.os }}-apt-${{ hashFiles('**/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-apt-

      - name: Install system dependencies (Ubuntu)
        if: matrix.platform.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Cargo fmt check
        working-directory: ./src-tauri
        run: cargo fmt --all -- --check

      - name: Cargo clippy
        working-directory: ./src-tauri
        run: cargo clippy --all-targets --target ${{ matrix.platform.target }} -- -D warnings

      - name: Cargo test
        working-directory: ./src-tauri
        run: cargo test --target ${{ matrix.platform.target }}

      - name: Cargo build
        working-directory: ./src-tauri
        run: cargo build --target ${{ matrix.platform.target }}
